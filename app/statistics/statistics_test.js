'use strict';

describe('myApp.statistics module', function() {

  beforeEach(module('myApp.statistics'));

  describe('statistics controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('StatisticsCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});