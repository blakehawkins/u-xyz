'use strict';

describe('myApp.submit module', function() {

  beforeEach(module('myApp.submit'));

  describe('submit controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('SubmitCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});