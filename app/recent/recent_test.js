'use strict';

describe('myApp.recent module', function() {

  beforeEach(module('myApp.recent'));

  describe('recent controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('RecentCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});